{
  "name": "meta-fuse",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone virtual filesystem service with FUSE and WebDAV",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "start": "pnpm --filter @meta-fuse/core start",
    "start:core": "pnpm --filter @meta-fuse/core start",
    "start:ui": "pnpm --filter @meta-fuse/ui dev",
    "test": "pnpm --filter @meta-fuse/core test",
    "lint": "pnpm -r lint"
  },
  "license": "MIT"
}
