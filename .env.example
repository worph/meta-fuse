# meta-fuse Environment Configuration

# Data path (shared with meta-sort)
DATA_PATH=/path/to/shared/data

# User/Group IDs for file permissions
PUID=1000
PGID=1000

# VFS refresh interval (milliseconds)
VFS_REFRESH_INTERVAL=30000
